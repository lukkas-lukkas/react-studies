import React from 'react';

class Button extends React.Component {
    render() {
        return (
            <button>
                Foo Button
            </button>
        )
    }
}

export default Button;
